#!/bin/bash

# 如果任何命令执行失败，立即退出脚本
set -e

# --- 关键步骤: 切换到脚本文件所在的目录 ---
# 这确保了无论从哪里调用此脚本，它总是在正确的项目根目录中运行。
cd "$(dirname "$0")"
echo "当前工作目录已切换到: $(pwd)"

# --- 调用 Python 脚本执行所有构建任务 ---
echo "正在启动 Python 构建脚本..."
python build.py
